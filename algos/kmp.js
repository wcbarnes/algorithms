/*
 * Recreate the Knuth-Morris-Pratt Algorithm
 * It takes a word and string and detects if that word exists
 * as a substring of the string.
 * When a mismatch occurs the alogrithm know the next best place to start based
 * how far search got before a mismatch.
 */

function kmp(word, string) {

}

module.exports = kmp;
