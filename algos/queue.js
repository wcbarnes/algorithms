/*
 * Create a Queue with the following methods
 * a. enqueue
 * b. dequeue
 * c. forEach
 */

function Queue() {
  this.store = [];
  this.length = 0;
}

Queue.prototype.enqueue = function enqueue(value) {

};

Queue.prototype.dequeue = function dequeue() {

};

Queue.prototype.forEach = function forEach(cb) {

};

module.exports = Queue;
