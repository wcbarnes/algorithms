const expect = require('expect.js');
const Heap = require('../algos/heap.js');


describe('Heap Datastructure', () => {
  it('should be a function', () => {
    expect(Heap).to.be.a('function');
  });
});
