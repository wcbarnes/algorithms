const expect = require('expect.js');
const Trie = require('../algos/trie.js');


describe('Trie Datastructure', () => {
  it('should be a function', () => {
    expect(Trie).to.be.a('function');
  });
});
