const expect = require('expect.js');
const Tree = require('../algos/tree.js');


describe('Tree Datastructure', () => {
  it('should be a function', () => {
    expect(Tree).to.be.a('function');
  });
});
