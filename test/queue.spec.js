const expect = require('expect.js');
const Queue = require('../algos/queue.js');


describe('Queue Datastructure', () => {
  it('should be a function', () => {
    expect(Queue).to.be.a('function');
  });
});
