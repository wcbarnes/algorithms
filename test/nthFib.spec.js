const expect = require('expect.js');
const nthFib = require('../algos/nthFib.js');


describe('nthFib should sort the array', () => {
  it('should be a function', () => {
    expect(nthFib).to.be.a('function');
  });
});
