const expect = require('expect.js');
const LinkedList = require('../algos/linked-list.js');


describe('Linked List Datastructure', () => {
  it('should be a function', () => {
    expect(LinkedList).to.be.a('function');
  });
});