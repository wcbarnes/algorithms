const expect = require('expect.js');
const graph = require('../algos/graph.js');


describe('Graph Datastructure', () => {
  it('should be a function', () => {
    expect(graph).to.be.a('function');
  });
});
