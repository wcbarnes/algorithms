const expect = require('expect.js');
const LinkedList = require('../algos/linked-list-dummy.js');


describe('Linked List Dummy Datastructure', () => {
  it('should be a function', () => {
    expect(LinkedList).to.be.a('function');
  });
});
