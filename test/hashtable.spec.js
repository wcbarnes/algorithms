const expect = require('expect.js');
const HashTable = require('../algos/hashtable.js');


describe('HashTable Datastructure', () => {
  it('should be a function', () => {
    expect(HashTable).to.be.a('function');
  });
});
