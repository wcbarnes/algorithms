const expect = require('expect.js');
const Stack = require('../algos/stack.js');


describe('Stack Datastructure', () => {
  it('should be a function', () => {
    expect(Stack).to.be.a('function');
  });
});
